# Next.js
.next/
out/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# Local env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS
.DS_Store
Thumbs.db

# Firebase Admin SDK service-account keys — NEVER commit these (they grant full
# project access and bypass all security rules). Keep them outside the repo and
# point GOOGLE_APPLICATION_CREDENTIALS at the local path instead.
serviceAccountKey.json
serviceAccount*.json
*firebase-adminsdk*.json
